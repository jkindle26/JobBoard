@model Microsoft.AspNet.Identity.EntityFramework.IdentityRole
<section class="masthead team text-white" style="padding:10rem 5rem ;">
    @{
        ViewBag.Title = "Delete";
    }

    <h2>Delete.</h2>

    <h3>Are you sure you want to delete this Role? </h3>
    <p>Deleting this Role will remove all users from this role. It will not delete the users.</p>
    <div>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>
        </dl>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-primary js-scroll-trigger" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
    </section>
